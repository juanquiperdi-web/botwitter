# === SECRETOS Y SESIÓN (NUNCA subir) ===
.env
.env.*
!.env.example
chrome_profile/

# === Logs y caché ===
*.log
__pycache__/
*.pyc
*.pyo

# === Estado de runtime (específico de cada máquina) ===
publish_state.json
following_state.json
x_radar_state.json
match_fixtures.json
match_published.json
news_cache.json

# === Restos / temporales ===
debug/
_archive_unused/
_cola_lote.json

# Cola de contenido: estado LOCAL (curador local la llena, bot local la consume).
content_queue.json
_cola_lote.json
curador.log

# Config local de Claude Code (no compartir)
.claude/settings.local.json
